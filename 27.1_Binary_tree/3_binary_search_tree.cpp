/* In binary search tree all the left subtree element should be less than root data and all the right subtree element should be greater than root data. this is called binary search tree property
this property should be satisfied at every node in the tree



*/

#include<bits/stdc++.h>
#include<iostream>
using namespace std;

struct bst{
    int data;
    bst *left;
    bst *right; 
};

bst* fbst(bst *root,int data) {

}

int main()
{
    struct bst* root=NULL;
    
    return 0;
}